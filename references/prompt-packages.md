# Prompt Packages

Prompt packages are for prompt output only.

Use them when:

- Henry explicitly asks for reusable prompt text
- real image delivery is blocked after a checked CLI attempt

A prompt package should include:

- original prompt
- compiled task summary
- recommended size
- recommended ratio
- negative prompt
- one generic prompt string
- validation checklist
- assumptions
