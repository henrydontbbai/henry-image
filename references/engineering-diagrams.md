# Exact-Dimension Work

Use raster output for concept review only.

Switch to SVG, PDF, or a written specification when the request needs:

- exact dimensions
- manufacturing notes
- repeated structural consistency
- unambiguous geometry

If two raster attempts still leave geometry unclear, stop generating more images and change the output form.
